# 工具使用说明

工具签名会通过函数调用自动提供。
这个文件记录那些不明显的约束和使用模式。

## exec — 安全限制

- 命令有可配置超时（默认 60 秒）
- 危险命令会被拦截（`rm -rf`、格式化、`dd`、关机等）
- 输出会在 10,000 个字符处截断
- `restrictToWorkspace` 配置可以把文件访问限制在工作区内

## cron — 定时提醒

- 用法请参考 cron 技能。
